apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gc-webreact
  namespace: #{ENV}#
spec:
  ingressClassName: webapprouting.kubernetes.azure.com
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: gc-webreact
            port:
              number: 80