{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "aksName": { "type": "string" },
    "location": { "type": "string", "defaultValue": "canadacentral" },
    "vmSize": { "type": "string", "defaultValue": "Standard_B2s_v2" },
    "nodeCount": { "type": "int", "defaultValue": 1 },
    "adminUsername": { "type": "string" },
    "sshRSAPublicKey": { "type": "string" }
  },
  "resources": [
    {
      "type": "Microsoft.ContainerService/managedClusters",
      "apiVersion": "2024-05-01",
      "name": "[parameters('aksName')]",
      "location": "[parameters('location')]",
      "sku": {
        "name": "Base",
        "tier": "Free"
      },
      "identity": {
        "type": "SystemAssigned"
      },
      "properties": {
        "dnsPrefix": "[parameters('aksName')]",
        "agentPoolProfiles": [
          {
            "name": "nodepool1",
            "mode": "System",
            "count": "[parameters('nodeCount')]",
            "vmSize": "[parameters('vmSize')]",
            "osType": "Linux",
            "type": "VirtualMachineScaleSets"
          }
        ],
        "linuxProfile": {
          "adminUsername": "[parameters('adminUsername')]",
          "ssh": {
            "publicKeys": [
              {
                "keyData": "[parameters('sshRSAPublicKey')]"
              }
            ]
          }
        }
      }
    }
  ]
}